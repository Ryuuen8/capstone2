"""
Minimal A* module placeholder.

This file currently contains only a stub `Cell` class. The project uses
NetworkX's A* implementation in `views.py` for pathfinding; keep this file
if you plan to add a custom A* implementation later.
"""

import math
import heapq


class Cell:
    """Small placeholder representing a grid cell for a future A* impl.

    Keep minimal: real routing uses NetworkX in `views.py`.
    """
    pass